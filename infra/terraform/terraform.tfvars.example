# Copy to terraform.tfvars and adjust as needed.
# terraform apply is NOT expected to be run for this assessment - see README.md.

aws_region   = "ap-southeast-1"
environment  = "dev"
project_name = "resale-datasg"
